# Component Showcase

A single page rendering every themed component, for visual review during development.

## Headings

# H1
## H2
### H3
#### H4
##### H5
###### H6

## Text formatting

Plain text, **bold text**, *italic text*, ***bold italic***, ~~strikethrough~~, and `inline code`.

## Links

[Internal link](../getting-started.md) and [external link](https://draculatheme.com).

## Lists

- Unordered item one
- Unordered item two
    - Nested item

1. Ordered item one
2. Ordered item two
    1. Nested ordered item

## Blockquote

> A blockquote spanning
> multiple lines.

## Code block

```python
def hello(name: str) -> str:
    return f"Hello, {name}!"
```

## Table

| Column A | Column B | Column C |
| -------- | -------- | -------- |
| 1        | 2        | 3        |
| foo      | bar      | baz      |

## Admonitions

!!! note
    A `note` admonition.

!!! abstract
    An `abstract` / `summary` / `tldr` admonition.

!!! info
    An `info` admonition.

!!! tip
    A `tip` / `hint` / `important` admonition.

!!! success
    A `success` / `check` / `done` admonition.

!!! question
    A `question` / `help` / `faq` admonition.

!!! warning
    A `warning` / `caution` / `attention` admonition.

!!! failure
    A `failure` / `fail` / `missing` admonition.

!!! danger
    A `danger` / `error` admonition.

!!! bug
    A `bug` admonition.

!!! example
    An `example` admonition.

!!! quote
    A `quote` / `cite` admonition.

!!! warning "Custom title"
    Admonitions also accept a custom title.

## Footer

Controlled by `theme.show_footer` in `mkdocs.yml` — visible at the bottom of this page unless disabled.
